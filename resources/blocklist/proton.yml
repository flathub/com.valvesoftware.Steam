entries:
  - basedir: "*(/*|/.*)/Proton 3.7/dist"
    blocklists:
      - binaries:
          - path: bin/wine64
            libdir: lib64
          - path: bin/wine64-preloader
            libdir: lib64
        libraries:
          - path: libFAudio.so

  - basedir: "*(/*|/.*)/Proton 3.7 Beta/dist"
    blocklists:
      - binaries:
          - path: bin/wine64
            libdir: lib64
          - path: bin/wine64-preloader
            libdir: lib64
        libraries:
          - path: libFAudio.so

  - basedir: "*(/*|/.*)/Proton 3.16/dist"
    blocklists:
      - binaries:
          - path: bin/wine64
            libdir: lib64
          - path: bin/wine64-preloader
            libdir: lib64
        libraries:
          - path: libFAudio.so

  - basedir: "*(/*|/.*)/Proton 3.16 Beta/dist"
    blocklists:
      - binaries:
          - path: bin/wine64
            libdir: lib64
          - path: bin/wine64-preloader
            libdir: lib64
        libraries:
          - path: libFAudio.so

  - basedir: "*(/*|/.*)/Proton 4.2/dist"
    blocklists:
      - binaries:
          - path: bin/wine64
            libdir: lib64
          - path: bin/wine64-preloader
            libdir: lib64
        libraries:
          - path: libFAudio.so.0.19.06

  - basedir: "*(/*|/.*)/Proton 4.11/dist"
    blocklists:
      - binaries:
          - path: bin/wine64
            libdir: lib64
          - path: bin/wine64-preloader
            libdir: lib64
        libraries: &FAudio-libraries
          - path: libFAudio.so.0.19.08
          - path: libFAudio.so.0.19.09
          - path: libFAudio.so.0.19.11
          - path: libFAudio.so.0.19.12
          - path: libFAudio.so.0.20.02
          - path: libFAudio.so.0.20.03
          - path: libFAudio.so.0.20.04
          - path: libFAudio.so.0.20.05
          - path: libFAudio.so.0.20.06
          - path: libFAudio.so.0.20.07
          - path: libFAudio.so.0.20.08
          - path: libFAudio.so.0.20.09
          - path: libFAudio.so.0.20.10

  - basedir: "*(/*|/.*)/Proton 5.0/dist"
    blocklists:
      - binaries:
          - path: bin/wine64
            libdir: lib64
          - path: bin/wine64-preloader
            libdir: lib64
        libraries: *FAudio-libraries
